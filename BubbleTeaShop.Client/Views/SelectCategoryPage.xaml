<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:BubbleTeaShop.Client.ViewModels"
             x:Class="BubbleTeaShop.Client.Views.SelectCategoryPage"
             Title="Select Menu Category">

    <Grid RowDefinitions="Auto, *, Auto">
        <Label 
            Text="Select Menu Category"
            FontSize="24"
            FontAttributes="Bold"
            HorizontalOptions="Center"
            Margin="20"
            Grid.Row="0"/>
        
        <CollectionView 
            Grid.Row="1"
            ItemsSource="{Binding Categories}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Frame Margin="10,5" Padding="15" CornerRadius="10" BackgroundColor="#E0F7FA" HasShadow="True">
                        <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10" VerticalOptions="CenterAndExpand">
                            <Label Text="{Binding .}" 
                                   FontSize="18" FontAttributes="Bold" TextColor="#006064" VerticalOptions="Center"/>
                            <Button Grid.Column="1"
                                    Text="Select"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:SelectCategoryViewModel}}, Path=SelectCategoryCommand}"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#00838F"
                                    TextColor="White"
                                    CornerRadius="8"
                                    Padding="10,5"
                                    HorizontalOptions="End"
                                    VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2"
                Text="Cancel"
                Command="{Binding CancelCommand}"
                BackgroundColor="#D32F2F"
                TextColor="White"
                CornerRadius="10"
                Padding="15,10"
                Margin="20"
                HorizontalOptions="End"
                VerticalOptions="End"/>
    </Grid>

</ContentPage>
